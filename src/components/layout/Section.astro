---
import { cn } from "@/lib/utils";

interface Props {
  id: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class:list={cn([className, "relative"])}><slot /></section>
