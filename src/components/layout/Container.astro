---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={cn([`w-full max-w-screen-xl mx-auto px-5 relative`, className])}><slot /></div>
